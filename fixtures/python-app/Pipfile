[[source]]
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
stripe = ">=7.0"
boto3 = "*"
requests = {version = ">=2.31", extras = ["security"]}
django = "~=4.2"

[dev-packages]
pytest = "*"
flake8 = ">=6.0"

[requires]
python_version = "3.11"
